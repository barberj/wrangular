<div ng-app="flapperNews" ng-controller="MainCtrl">
  <div ng-repeat="post in posts | orderBy: '-upvotes'">
    {{post.title}} - upvotes: {{post.upvotes}}
  </div>
  <a class="btn btn-default" ng-click="addPost()">Post</a>
</div>
